import 'package:flutter/material.dart';

class PantryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Dolabım')),
      body: Center(child: Text('Malzemelerin listesi burada olacak')),
    );
  }
}
